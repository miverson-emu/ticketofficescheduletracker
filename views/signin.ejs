<html>
	<head>
		<script>

			function populateList(data) {
				data.map(item => {
					return selectOption(item.firstname + " " + item.lastname, item.lastname + ", " + item.firstname)
				})
			}

			function selectOption(value, display) {
				return "<option value =" + value  + ">" + display + "</option>"
			}

			window.onload = () => {
				populateList(<%= worker%>)
			}
		</script>
	</head>

<body>
<h1><%= data %></h1>
<form action = "/validate" method = "POST">
	<select name = 'name'>
		<option disabled selected> -- Select your Name</option>
	</select>

	<input placeholder = 'Passkey' name = 'id' type = text value = "E">
	<!-- LOOK FOR 'X' password -->

	<input type = 'submit'>
</form>
</body>
</html>